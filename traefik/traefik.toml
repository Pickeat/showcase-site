[entryPoints]
  [entryPoints.http]
  address = ":80"
  [entryPoints.https]
  address = ":443"

[api]

[providers.docker]
  endpoint = "unix:///var/run/docker.sock"

[certificatesResolvers.pickeat.acme]
  email = "julien.montagne@epitech.eu"
  storage = "acme.json"
  [certificatesResolvers.pickeat.acme.httpChallenge]
    entryPoint = "http"


[retry]

[docker]
endpoint = "unix:///var/run/docker.sock"
domain = "pickeat.fr"
watch = true
exposedbydefault = false
